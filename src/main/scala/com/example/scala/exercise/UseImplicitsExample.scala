package com.example.scala.exercise

class UseImplicitsExample {

  //import ImplicitExample._
   var x=6
  //when we use nonEmpty method on some type T then it gets converted to Option[T] by the implicit function been imported
  print(x)
}
